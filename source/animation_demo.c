#include "../include/animation.h"

int main(void) {
    /* Run the animation demo. This is a separate executable and
       does not modify your existing program files. */
    animation_run_demo();
    return 0;
}
